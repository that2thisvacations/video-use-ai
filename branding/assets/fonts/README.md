# Fonts

Expected usage:

- brand-approved type references
- optional local font files only if licensing permits

Recommended formats:

- `OTF`
- `TTF`

Notes:

- keep this folder for references and approved assets, not ad hoc installs
- the demo wrapper only needs to know the intended type direction for now
- future render hooks can map this folder into subtitle and overlay styling
